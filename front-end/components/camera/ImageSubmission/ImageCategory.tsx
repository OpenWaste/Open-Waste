import * as React from "react";
import { View } from "react-native";
import { Text } from "native-base"

export function ImageCategory({ }) {
  return (
    <View>
        <Text color = "#0F968D">Submission</Text>
    </View>
  );
}